# 0x-wallet

### Setup

Start backend: `yarn start` or `nodemon`
Start frontend: `yarn dev`

Supported networks:

-   Mainnet
-   Matic
-   Arbitrum

.env example:

```
API_KEY=<apiKey>
NETWORK=ETH_MAINNET
#NETWORK=MATIC_MAINNET
```
